# Nom de l'application (OK)
spring.application.name=QManager

# ?? Retirer les vues JSP si tu es 100% en React
# (Tu peux les supprimer ou les commenter)
# spring.mvc.view.prefix=/WEB-INF/jsp/
# spring.mvc.view.suffix=.jsp

# ?? Connexion à MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/qmanager_db?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=

# ? Configuration JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

spring.jpa.properties.hibernate.format_sql=true
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql=TRACE
